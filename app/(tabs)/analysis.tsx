import {Text} from 'react-native'; import {Screen,s} from '../../src/components/Screen'; import {useSubscriptionStore} from '../../src/stores/subscriptionStore'; import {PremiumGate} from '../../src/components/PremiumGate'; import {useAuthStore} from '../../src/stores/authStore'; export default function Analysis(){const premium=useSubscriptionStore(x=>x.isPremium);const user=useAuthStore(x=>x.user);return <Screen><Text style={s.title}>Kişisel Analiz</Text><PremiumGate premium={premium}><Text style={s.text}>{user?.name||'Sen'} için natal harita, Jung arketipi, transitler ve Tarot birleşiminden kişiselleştirilmiş analiz.</Text></PremiumGate></Screen>}
