import {Redirect} from 'expo-router'; import {useAuthStore} from '../src/stores/authStore'; export default function Index(){const {user,isGuest}=useAuthStore();return <Redirect href={user&&!isGuest?'/(tabs)':'/(auth)/welcome'}/>}
