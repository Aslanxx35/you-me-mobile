import {View,Text,TouchableOpacity} from 'react-native'; import {router} from 'expo-router'; import {Screen,s} from '../../src/components/Screen'; import {useAuthStore} from '../../src/stores/authStore'; import {COLORS} from '../../src/constants/colors'; import {useLanguageStore} from '../../src/stores/languageStore';
export default function Welcome(){const setGuest=useAuthStore(x=>x.setGuest);const lang=useLanguageStore(x=>x.language);const setLang=useLanguageStore(x=>x.setLanguage);return <Screen><View style={{flex:1,minHeight:650,justifyContent:'center'}}><Text style={[s.title,{textAlign:'center',fontSize:40}]}>YOU me</Text><Text style={[s.subtitle,{textAlign:'center'}]}>Gökyüzünü değil, kendini keşfet.</Text><TouchableOpacity style={s.button} onPress={()=>router.push('/(auth)/login')}><Text style={s.buttonText}>Giriş Yap</Text></TouchableOpacity><TouchableOpacity style={[s.button,{backgroundColor:COLORS.dark.panel}]} onPress={()=>router.push('/(auth)/register')}><Text style={[s.buttonText,{color:COLORS.dark.gold}]}>Kayıt Ol</Text></TouchableOpacity><View style={{flexDirection:'row',justifyContent:'center',gap:8,marginTop:12}}>{(['tr','en','de'] as const).map(l=><TouchableOpacity key={l} onPress={()=>setLang(l)} style={{padding:8}}><Text style={{color:lang===l?COLORS.dark.gold:COLORS.dark.textDim}}>{l.toUpperCase()}</Text></TouchableOpacity>)}</View><TouchableOpacity onPress={()=>{setGuest();router.replace('/(auth)/onboarding')}} style={{padding:16}}><Text style={{textAlign:'center',color:COLORS.dark.textDim}}>Misafir olarak devam et</Text></TouchableOpacity></View></Screen>}
