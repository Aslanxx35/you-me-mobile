import * as Linking from 'expo-linking'; import {router} from 'expo-router';
export function handleDeepLink(url:string){const parsed=Linking.parse(url); const path=parsed.path||''; if(path==='tarot')router.push('/(tabs)/tarot'); else if(path==='daily')router.push('/(tabs)/daily'); else if(path==='synastry')router.push('/(tabs)/synastry'); else if(path==='transit')router.push('/(tabs)/transit'); else if(path==='natal')router.push('/(tabs)/natal'); else if(path==='reset-password')router.push({pathname:'/(auth)/reset-password',params:{token:String(parsed.queryParams?.token||'')}});}
