import {View,Text,TouchableOpacity} from 'react-native'; import {COLORS} from '../constants/colors'; export function StateView({message,onRetry}:{message:string;onRetry?:()=>void}){return <View style={{padding:30,alignItems:'center'}}><Text style={{color:COLORS.dark.text,textAlign:'center'}}>{message}</Text>{onRetry&&<TouchableOpacity onPress={onRetry} style={{marginTop:16,padding:12,borderRadius:10,backgroundColor:COLORS.dark.gold}}><Text style={{fontWeight:'700'}}>Tekrar Dene</Text></TouchableOpacity>}</View>}
